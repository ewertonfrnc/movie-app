import { COLORS } from './colors.constant';
import { SPACING } from './spacing.constant';
import { SHADOWS } from './shadows.constant';
import { SIZES } from './sizes.constants';
import { FONTS } from './fonts.constant';

export const theme = {
  COLORS,
  SPACING,
  SHADOWS,
  SIZES,
  FONTS,
};
